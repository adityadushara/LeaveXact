version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: leavexact-backend
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:///./data/leavexact.db
      - SECRET_KEY=your-secret-key-change-in-production-min-32-chars
      - DEBUG=true
      - ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # PostgreSQL for production (optional)
  # postgres:
  #   image: postgres:15-alpine
  #   container_name: leavexact-postgres
  #   environment:
  #     POSTGRES_DB: leavexact
  #     POSTGRES_USER: leavexact
  #     POSTGRES_PASSWORD: change-this-password
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"
  #   restart: unless-stopped
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U leavexact"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5

volumes:
  postgres_data:
